version: '3.8'

services:
  # L'Base de donnees hna f 10.10.10.25 dyalk (Nta houwa l'm3elem hna)
  db:
    image: postgres:15-alpine
    container_name: pilot_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: pilot_user
      POSTGRES_PASSWORD: pilot_password
      POSTGRES_DB: pilotdb
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - pilot-network

  backend:
    build:
      context: ./pilot
      dockerfile: Dockerfile
    container_name: pilot_backend
    restart: unless-stopped
    ports:
      - "6225:6225"
    environment:
      - SERVER_PORT=6225
      # Ki-pointi 3la l'container dyal db li zedna l'fo9
      - SPRING_DATASOURCE_URL=jdbc:postgresql://db:5432/pilotdb
      - SPRING_DATASOURCE_USERNAME=pilot_user
      - SPRING_DATASOURCE_PASSWORD=pilot_password
      - SPRING_JPA_HIBERNATE_DDL_AUTO=update
    depends_on:
      - db
    networks:
      - pilot-network

  frontend:
    build:
      context: ./pilot-frontend
      dockerfile: Dockerfile
      args:
        - NEXT_PUBLIC_API_URL=http://10.10.10.25:6225
        - NEXT_PUBLIC_WS_URL=http://10.10.10.25:6225/ws
    container_name: pilot_frontend
    restart: unless-stopped
    ports:
      - "2227:3000"
    depends_on:
      - backend
    networks:
      - pilot-network

volumes:
  pgdata:

networks:
  pilot-network:
    driver: bridge